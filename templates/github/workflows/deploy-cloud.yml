name: Deploy Laravel Cloud
on:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: deploy-cloud-production
  cancel-in-progress: false
jobs:
  quality:
    uses: ./.github/workflows/quality.yml
    secrets: inherit
  deploy:
    needs: quality
    if: github.ref == 'refs/heads/main'
    runs-on: blacksmith-2vcpu-ubuntu-2404-arm
    timeout-minutes: 5
    environment: production
    steps:
      - name: Report unconfigured deployment
        if: vars.DEPLOY_CLOUD_ENABLED != 'true'
        run: echo 'Quality passed. Laravel Cloud deployment is unconfigured; connect the project and enable its deployment hook.'
      - name: Trigger the manually connected Laravel Cloud environment
        if: vars.DEPLOY_CLOUD_ENABLED == 'true'
        env:
          DEPLOY_HOOK_URL: ${{ secrets.LARAVEL_CLOUD_DEPLOY_HOOK_URL }}
        run: |
          test -n "$DEPLOY_HOOK_URL" || { echo 'Configure the Laravel Cloud deployment hook first' >&2; exit 1; }
          curl --fail --silent --show-error --output /dev/null --request POST "$DEPLOY_HOOK_URL"
