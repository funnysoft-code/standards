# peca-certa design SSOT Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Every named peca-certa screen has DESIGN.md, an HTML mock, Mobbin citations, and desktop/phone route screenshots.

**Architecture:** `design/` at repo root. `design-mock` then `frontend-ui` skills. No brand rewrite. Theme stays system/light/dark.

**Tech Stack:** HTML mocks, `scripts/screenshot.sh` from the stamp, Mobbin MCP, Herd `https://peca-certa.test`.

**Spec:** design loop + spec issue 4
**Issue:** [F7T-130](https://linear.app/funnysoft/issue/F7T-130)
**Blocked by:** F7T-129 (so routes still match after folder moves)

## Global Constraints

- Branch `feat/f7t-130-design-ssot`
- Viewports 1440x900 and 390x844
- Mobbin first. If MCP unauthorized, stop.
- No purple gradient, Inter, or three equal feature cards
- No Apex Space Grotesk / `#e62425` unless DESIGN.md adopts them (it should not)
- No i18n extraction. No dark-first retrofit
- Adversary treats missing mock/route PNGs as Critical

## Screens (slug = route)

| Slug | Route |
| --- | --- |
| login | `/login` |
| parts-index | `/parts` |
| parts-show | `/parts/{searchRun}` |
| chat-index | `/chat` |
| analytics-index | `/analytics` |
| settings-profile | `/settings/profile` |
| settings-security | `/settings/security` |
| settings-appearance | `/settings/appearance` |
| settings-suppliers | `/settings/suppliers` |
| admin-users | `/admin/users` |
| admin-roles | `/admin/roles` |
| admin-dashboard | `/admin` |
| welcome | `/` (if still served) |

Auth screens (forgot/reset/2fa/verify) get mocks if they are user-facing in production.

---

### Task 1: DESIGN.md

- [ ] **Step 1:** Expand `design/DESIGN.md` from the thin F7T-128 file. Record typeface actually used (whatever shadcn/new-york uses today), surfaces, radius, appearance cookie. Tokens in `design/tokens.css` only if values are extracted from `resources/css/app.css`. Do not invent a new palette.
- [ ] **Step 2:** Commit `docs: write peca-certa DESIGN.md seed`

---

### Task 2: Mock each slug

For each slug:

- [ ] **Step 1:** Mobbin search. Put URLs in an HTML comment at the top of `design/mocks/<slug>/index.html`.
- [ ] **Step 2:** If no owner direction, write `index-a.html` and `index-b.html` for the first new visual (parts-index only). Owner picks. Others may follow the existing UI (mock documents current, not a redesign).
- [ ] **Step 3:** Shoot mock.png / mock-mobile.png via `scripts/screenshot.sh`.
- [ ] **Step 4:** Implement nothing unless the mock revealed a bug. Then shoot route.png / route-mobile.png from `https://peca-certa.test/...`.
- [ ] **Step 5:** Commit per slug `docs: add <slug> mock and route shots`

Login to Herd as a real user. Do not commit session cookies. Do not put secrets in mocks.

---

### Task 3: PR

- [ ] **Step 1:** Adversary. Missing PNG = Critical.
- [ ] **Step 2:** PR citing F7T-130.
